query getCategoryById($id: ID!) {
  category(id: $id) {
    id
    name
    image

    products {
      id
      title
      price
      description
      images
      creationAt
      updatedAt
    }
  }
}
